import React from "react";
import Layout from "./Layout";
const products = () => {
  return (
    <Layout>
      <div className=" flex">
        <button>Add a Product</button>
      </div>
    </Layout>
  );
};

export default products;
